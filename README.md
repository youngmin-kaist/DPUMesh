# DPUMesh

## Host library

The DPUmesh native API, POSIX preload shim and C++/Go gRPC adapters run on the
host over this transport; see [design/HOST.md](design/HOST.md) for the layout,
the mapping onto the DPUMesh wire, limits and configuration.
